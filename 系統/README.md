# 🐾 寵物農場日記系統 - 設定說明

## 📋 專案簡介
這是一個可愛的寵物農場日記系統，每次寫日記就能獲得一隻隨機的寵物（狗狗🐶或貓咪🐱）！如果連續3天沒寫日記，就會失去一隻寵物喔！

## 🎯 主要功能
1. ✍️ **每日日記** - 每天寫一篇日記，獲得一隻新寵物
2. 📌 **臨時筆記** - 隨時記錄重要事項
3. 🐾 **寵物農場** - 收集和展示你的寵物們
4. 📊 **統計追蹤** - 顯示狗狗、貓咪數量和總日記數
5. ⚠️ **提醒機制** - 超過3天沒寫日記會失去寵物

## 🚀 Firebase 設定步驟

### 步驟 1: 建立 Firebase 專案
1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. 點擊「新增專案」
3. 輸入專案名稱：`pet-farm-daily-journal`
4. 按照提示完成專案建立

### 步驟 2: 啟用 Firestore
1. 在左側選單點擊「Firestore Database」
2. 點擊「建立資料庫」
3. 選擇「以測試模式啟動」（稍後可以修改規則）
4. 選擇資料庫位置（建議選擇 asia-east1 或 asia-northeast1）

### 步驟 3: 設定 Firestore 安全規則
在 Firestore 規則分頁，設定以下規則：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允許所有讀寫操作（僅用於開發測試）
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

**注意：** 這是測試用的規則，正式環境請使用更嚴格的安全規則！

### 步驟 4: 取得 Firebase 配置
1. 在專案設定中點擊「網頁」圖示 (</>)
2. 註冊應用程式
3. 複製 Firebase 配置資訊

### 步驟 5: 更新程式碼
在 `index.html` 檔案中，找到以下程式碼並替換成你的 Firebase 配置：

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",                    // 替換這裡
    authDomain: "pet-farm-daily-journal.firebaseapp.com",
    projectId: "pet-farm-daily-journal",       // 確認專案ID
    storageBucket: "pet-farm-daily-journal.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",       // 替換這裡
    appId: "YOUR_APP_ID"                       // 替換這裡
};
```

## 📁 Firestore 資料結構

系統會自動建立以下 Collections：

### 1. `pets` Collection
儲存所有寵物資料
```javascript
{
  type: "🐶" or "🐱",
  createdAt: Timestamp
}
```

### 2. `notes` Collection
儲存臨時筆記
```javascript
{
  content: "筆記內容",
  createdAt: Timestamp
}
```

### 3. `diaries` Collection
儲存日記
```javascript
{
  content: "日記內容",
  createdAt: Timestamp,
  petReward: "🐶" or "🐱"
}
```

### 4. `stats` Document
儲存全域統計（路徑：stats/global）
```javascript
{
  dogs: 0,
  cats: 0,
  totalDiaries: 0,
  lastEntryDate: Timestamp
}
```

## 🎨 檔案說明

- **index.html** - 主HTML檔案，包含頁面結構和Firebase初始化
- **styles.css** - 樣式檔案，包含可愛的寵物農場風格設計
- **app.js** - 主要JavaScript邏輯，處理所有功能和Firebase互動

## 💡 使用方式

1. 將三個檔案放在同一個資料夾
2. 更新 `index.html` 中的 Firebase 配置
3. 使用本地伺服器開啟（建議使用 VS Code 的 Live Server 擴充功能）
4. 開始寫日記，收集可愛的寵物！

## 🌟 特色功能

### 寫日記獲得寵物
- 每天只能寫一篇日記
- 每次寫日記會隨機獲得🐶或🐱
- 會有可愛的動畫效果

### 懲罰機制
- 連續3天沒寫日記會失去一隻寵物
- 系統會顯示警告提示

### 臨時筆記
- 可以隨時新增筆記
- 不影響寵物數量
- 可以刪除不需要的筆記

## 🛠️ 故障排除

### 問題：無法連接到 Firebase
- 檢查 Firebase 配置是否正確
- 確認網路連線正常
- 檢查瀏覽器控制台的錯誤訊息

### 問題：寵物沒有顯示
- 確認 Firestore 資料庫已建立
- 檢查安全規則是否允許讀寫
- 查看瀏覽器控制台是否有錯誤

### 問題：日記無法儲存
- 確認今天是否已經寫過日記
- 檢查 Firestore 寫入權限
- 確認輸入內容不是空白的

## 📝 開發建議

### 本地測試
使用本地伺服器，不要直接開啟 HTML 檔案：
```bash
# 使用 Python
python -m http.server 8000

# 或使用 Node.js
npx http-server
```

### 安全性提升
正式環境請使用更嚴格的 Firestore 規則，例如：
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pets/{petId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /notes/{noteId} {
      allow read, write: if request.auth != null;
    }
    match /diaries/{diaryId} {
      allow read, write: if request.auth != null;
    }
    match /stats/{statId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

## 🎉 享受你的寵物農場日記之旅！

有任何問題歡迎隨時詢問！記得每天寫日記，讓你的寵物農場越來越熱鬧！ 🐾
